/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js,jsx}"], //what are the places where going to use tailwind. Any file with these extension in src we can use tailwind.
  theme: {
    extend: {},
  },
  plugins: [],
};
